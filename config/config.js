const config = {
    isDevModeEnabled: true,
    server: {
        hostName: "localhost",
        port: 5000
    },
    database: {
        host: "localhost",
        user: "root",
        password: "",
        database: "test"
    }
};

export default config;