const config = {
    isDevModeEnabled: true,
    server: {
        hostName: "localhost",
        port: 3001
    },
    database: {
        host: "localhost",
        user: "root",
        password: "",
        database: "test"
    }
};

export default config;